# Software architecture
Acquisition -> metadata -> coherent averaging/calibration/IFFT -> background -> migration -> CFAR -> feature extraction -> generic or trained classification -> A/B/C-scan and 3-D visualization -> export/report. Specific metal subtype is only emitted by a validated site-specific trained model.
